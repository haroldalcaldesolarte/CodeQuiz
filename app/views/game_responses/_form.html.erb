<%= form_with(model: game_response) do |form| %>
  <% if game_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_response.errors.count, "error") %> prohibited this game_response from being saved:</h2>

      <ul>
        <% game_response.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_session_id %>
    <%= form.text_field :game_session_id %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :correct %>
    <%= form.check_box :correct %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
